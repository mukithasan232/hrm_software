# Increase network timeouts and retries for unreliable CI/CD environments
fetch-timeout = 120000
fetch-retries = 5
fetch-retry-maxtimeout = 120000

# Use a faster, globally distributed registry mirror to improve download speeds
registry = "https://registry.yarnpkg.com"